{
  "name": "riddle-diary-web",
  "version": "0.1.0",
  "description": "An enchanted, handwritten AI diary running on Cloudflare Workers.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "wrangler": "^4.128.0"
  }
}
