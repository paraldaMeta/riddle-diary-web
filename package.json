{
  "name": "riddle-diary-web",
  "version": "0.3.0",
  "description": "A handwritten geomancy answer book running on Cloudflare Workers.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "test": "node tools/test-geomancy.mjs",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "wrangler": "^4.128.0"
  }
}
